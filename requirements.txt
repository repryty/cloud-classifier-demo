numpy>=1.26
pandas>=2.2
matplotlib>=3.8
pillow>=10.0
jupyter>=1.0
# 딥러닝 (실제 CNN 학습/추론). GPU 권장.
# 설치:
#   GPU(CUDA 12.8):  python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   CPU:             python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.11
torchvision>=0.26